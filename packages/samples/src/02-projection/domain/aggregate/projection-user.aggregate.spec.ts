import { ProjectionUserAggregate } from './projection-user.aggregate';

describe('ProjectionUserAggregate', () => {
  it('can be created', () => {
    const aggregate = new ProjectionUserAggregate();
  });
});
